query RecipesQuery { 
  recipes {
    description
    id
    image
    title
    recipeIngredients {
      id
      ingredientId
      measurementType
      measurementValue
      recipeId
    }
  }
}